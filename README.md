# Chopper Body V2

## Acknowledgments

This firmware was strongly influenced by the [Amidala](https://github.com/thePunderWoman/Amidala) firmware by [thePunderWoman](https://github.com/thePunderWoman).

ESP32-based body controller for the Chopper robot. Controls drive motors, dome rotation, body servos, MP3 audio, and a bubble machine via ESP-NOW commands from the controller.

## Features

- **MDDS30 drive motors** — PWM Independent mode, differential steering
- **Cytron MD10C dome rotation** — bidirectional speed control
- **Pololu Maestro servos** — scripted body animations via Serial1
- **MP3 audio player** — serial protocol playback via Serial2
- **Bubbles relay** — on/off control
- **ESP-NOW** — encrypted communication with the controller
- **WiFi AP + OTA** — web interface at `chopper.body` for firmware updates
- **Safety timeout** — motors stop after 500ms without commands or on ESP-NOW disconnect

## ESP32 Pin Assignments

| Function | GPIO | Type | Notes |
|---|---|---|---|
| **MDDS30 IN1** (left dir) | 27 | Digital out | LOW = forward, HIGH = reverse. 10k pull-down on board |
| **MDDS30 AN1** (left speed) | 14 | LEDC PWM | 5 kHz, 8-bit. 10k pull-down on board |
| **MDDS30 IN2** (right dir) | 12 | Digital out | LOW = forward, HIGH = reverse. 10k pull-down on board |
| **MDDS30 AN2** (right speed) | 13 | LEDC PWM | 5 kHz, 8-bit. 10k pull-down on board |
| **Cytron MD10C DIR** (dome dir) | 33 | Digital out | 10k pull-down on board |
| **Cytron MD10C PWM** (dome speed) | 25 | LEDC PWM | 5 kHz, 8-bit. 10k pull-down on board |
| **Dome home sensor** | 32 | Digital in | A3144 hall-effect, 10k+20k divider |
| **Dome encoder A** | 35 | Digital in | Input-only, 10k+20k divider |
| **Dome encoder B** | 34 | Digital in | Input-only, 10k+20k divider |
| **Pololu Maestro TX** | 23 | Serial1 TX | 9600 baud |
| **Pololu Maestro RX** | 22 | Serial1 RX | 1k + 2k voltage divider (5V -> 3.3V) |
| **MP3 Player TX** | 4 | Serial2 TX | 9600 baud |
| **MP3 Player RX** | 16 | Serial2 RX | 1k + 2k voltage divider (5V -> 3.3V) |
| **Bubbles relay** | 26 | Digital out | Relay module, active HIGH |
| **USB Debug TX** | 1 | Serial0 TX | 115200 baud |
| **USB Debug RX** | 3 | Serial0 RX | 115200 baud |

### MDDS30 DIP Switch Setting

`0b10110100` — PWM Independent mode. All three hardware UARTs remain free.

### Serial Port Allocation

| UART | GPIO TX | GPIO RX | Baud | Device |
|---|---|---|---|---|
| Serial0 | 1 | 3 | 115200 | USB debug console |
| Serial1 | 23 | 22 | 9600 | Pololu Maestro servo controller |
| Serial2 | 4 | 16 | 9600 | MP3 player module (Catalex-style, requires "select TF card" init) |

## Build

Requires [PlatformIO](https://platformio.org/).

Before your first build, set up ESP-NOW encryption keys — see
[Generating ESP-NOW keys](#generating-esp-now-keys) below. The build will
fail with a clear error if `.env` is missing.

```bash
# One-time setup: copy the template and fill in real keys
cp .env.example .env

# Compile firmware
pio run

# Upload to ESP32
pio run --target upload

# Run unit tests (native)
pio test -e native

# Monitor serial output
pio device monitor
```

## Project Structure

```
pcb/                                  # KiCad project (schematic, board, gerbers)
firmware/
  platformio.ini
  .env.example                       # Template for ESP-NOW keys — copy to .env
  partitions/ota_4MB.csv
  scripts/load_secrets.py            # Pre-build: .env -> include/secrets.h
  scripts/embed_web.py              # Pre-build: inlines CSS into HTML
  web/
    assets/common.css                # Shared styles
    index.html                       # Home page (info + links)
    update.html                      # OTA firmware upload page
  include/
    web_pages.h                      # AUTO-GENERATED by embed_web.py
    secrets.h                        # AUTO-GENERATED by load_secrets.py, gitignored
  src/
    config.h                         # Pin definitions, timeouts (keys come from secrets.h)
    main.cpp                         # Setup/loop, WiFi, command dispatch
    communication/
      MessageTypes.h                 # BodyCommand / BodyTelemetry structs
      EspNowController.h/.cpp        # ESP-NOW encrypted communication
    motor/
      MotorController.h/.cpp         # MDDS30 + L298N motor control
    servo/
      ServoController.h/.cpp         # Pololu Maestro body servos
    audio/
      AudioController.h/.cpp         # Serial MP3 player protocol
    web/
      WebController.h/.cpp           # WiFi AP, OTA updates, info API
      logger.h                       # Serial logging utility
  test/
    mocks/                           # Arduino/ESP32 mock headers
    test_message_types/              # Struct packing and field tests
    test_motor_controller/           # Drive mixing, dome, timeout tests
    test_audio_controller/           # MP3 protocol frame tests
    test_servo_controller/           # Animation state machine tests
    test_esp_now_controller/         # Connection timeout, dispatch tests
```

## ESP-NOW Protocol

### Controller -> Body (msgType = 3, 26 bytes)

| Field | Type | Description |
|---|---|---|
| msgType | int8 | Always 3 |
| status | int8 | 0 = buttons, 1 = movement, 2 = settings, -1 = connection |
| lx | int8 | Drive X axis: -100 to 100 |
| ly | int8 | Drive Y axis: -100 to 100 |
| domeSpeed | int8 | Dome rotation: -100 to 100 |
| audioTrack | uint8 | 0 = none, 1-255 = play track |
| volume | uint8 | 0-30 |
| bubbles | uint8 | 0 = off, 1 = on |
| button | char[8] | Servo button name (e.g. "y", "du") |
| macro | int16[4] | Servo macro IDs |
| connectionStatus | int8 | 0 = disconnected, 1 = connected |
| padding | uint8 | Alignment padding |

### Body -> Controller (msgType = 4, 8 bytes)

| Field | Type | Description |
|---|---|---|
| msgType | int8 | Always 4 |
| connected | uint8 | 1 if ESP-NOW link active |
| motorsActive | uint8 | 1 if drive/dome motors running |
| audioPlaying | uint8 | 1 if audio track playing |
| uptimeMs | uint32 | Milliseconds since boot |

## WiFi Access Point

- **SSID:** ChopperBody
- **IP:** 192.168.5.1
- **mDNS:** chopper-body.local
- **Pages:** `/` (home), `/update` (OTA)
- **API:** `/api/info` (JSON: version, uptime, heap, MAC)

## Generating ESP-NOW keys

This board talks to the other Chopper boards over encrypted ESP-NOW. Before
your first build, generate two 16-byte (128-bit) keys — a Primary Master Key
(PMK) and a Local Master Key (LMK) — and put them in a `.env` file
(gitignored, never commit it) at the project root:

```bash
cp .env.example .env
```

Generate two random 16-byte keys, each as 32 hex characters:

```bash
# Option 1: openssl
openssl rand -hex 16   # run twice — once for PMK_KEY, once for LMK_KEY

# Option 2: python
python3 -c "import secrets; print(secrets.token_hex(16))"
```

Edit `.env` and paste the values in:

```
PMK_KEY=<first random hex string>
LMK_KEY=<second random hex string>
```

**These two keys must be byte-for-byte identical across chopper body,
chopper head, and chopper receiver** — generate them once and copy the same
`.env` values into all three projects. Mismatched keys mean the boards can't
decrypt each other's ESP-NOW packets and the mesh will never connect.

`.env` also holds `CONTROLLER_MAC` — the controller's WiFi MAC address (as
hex, e.g. `C0CDD6CA29E0`), which must likewise be identical across all three
firmware projects — and `WIFI_AP_PASSWORD`, the password for this board's
local OTA-update access point (pick your own, at least 8 characters; unlike
the other values, this one doesn't need to match the other boards).

`scripts/load_secrets.py` runs automatically before every build (see
`platformio.ini`) and turns `.env` into `include/secrets.h`, which
`config.h` includes. Neither `.env` nor the generated `secrets.h` are
committed to git — the build fails with a clear error if `.env` is missing.
